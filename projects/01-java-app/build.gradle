//plugins {
//    id 'soramitsu'
//    id 'application'
//}

buildscript {
    repositories {
        mavenLocal()
        jcenter()
        gradlePluginPortal()
        dependencies { classpath 'jp.co.soramitsu:sora-plugin:+' }
    }
}
apply plugin: 'jp.co.soramitsu.sora-plugin'
apply plugin: 'application'
apply plugin: 'java'

println("Hello from 01-java-app")

repositories {
    jcenter()
}

dependencies {
    testImplementation 'junit:junit:4.12'
}

// Define the main class for the application
group = 'jp.co.soramitsu'
mainClassName = 'example.App'

soramitsu {
    docker {
        jar = new File("build/libs/${project.name}-${project.version}.jar")
    }
}

jar {
    manifest {
        attributes 'Implementation-Version': version,
                'Main-Class': 'example.App'
    }
}

            plugins {
                id 'jp.co.soramitsu.sora-plugin'   
            }
            group = 'group'
            
            soramitsu {
              docker {
                tag = 'test'
                jar = new File('jar.jar')
                customFiles = '[file:./projects/01-java-app/build.gradle]'
              }
            }
        